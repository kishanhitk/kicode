<section
  class="relative flex flex-col items-center justify-center px-4 py-16 sm:py-24 md:py-32 overflow-x-hidden bg-bg-primary w-full max-w-full"
>
  <div class="relative z-10 max-w-5xl w-full mx-auto text-center">
    <!-- Logo/Brand -->
    <div class="mb-6 animate-fade-in">
      <div
        class="inline-flex items-center justify-center px-3 py-1 rounded-full bg-bg-surface border border-border-default mb-8"
      >
        <span class="text-xs font-mono text-text-secondary"
          >Beta Now Available</span
        >
      </div>
      <h1
        class="text-4xl sm:text-5xl md:text-6xl lg:text-8xl font-bold text-text-primary tracking-tight mb-6 font-mono"
      >
        <span class="text-accent-green">></span>kicode<span
          class="text-accent-blue">_</span
        >
      </h1>
    </div>

    <!-- Tagline -->
    <p
      class="text-lg sm:text-xl md:text-2xl text-text-secondary mb-10 max-w-2xl mx-auto leading-relaxed animate-slide-up"
      style="animation-delay: 0.1s"
    >
      The open source AI coding agent. <br />
      <span class="text-text-muted">Built for your terminal.</span>
    </p>

    <!-- Install Command slot -->
    <div
      class="mb-12 animate-slide-up w-full max-w-2xl mx-auto"
      style="animation-delay: 0.2s"
    >
      <slot name="install-command" />
    </div>

    <!-- CTA Buttons -->
    <div
      class="flex flex-col sm:flex-row gap-4 justify-center animate-slide-up mb-16"
      style="animation-delay: 0.3s"
    >
      <a
        href="/getting-started/installation"
        class="inline-flex items-center justify-center px-8 py-3 bg-text-primary hover:bg-text-secondary text-bg-primary font-medium rounded-lg transition-colors"
      >
        Get Started
      </a>
      <a
        href="https://github.com/kishanhitk/kicode"
        target="_blank"
        rel="noopener noreferrer"
        class="inline-flex items-center justify-center px-8 py-3 border border-border-default hover:bg-bg-surface text-text-primary font-medium rounded-lg transition-colors"
      >
        GitHub
      </a>
    </div>

    <!-- Terminal Demo -->
    <div class="w-full animate-slide-up" style="animation-delay: 0.4s">
      <slot name="terminal-demo" />
    </div>
  </div>
</section>
